{
  "name": "neon-idle-rpg",
  "version": "1.0.0",
  "description": "Neon Idle RPG Mobile",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed for plain HTML project'",
    "copy": "cap copy",
    "open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
